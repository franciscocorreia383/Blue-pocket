<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bluepocket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.GraphActivity"></activity>
        <activity android:name=".view.activities.GraphicAllMovActivity" />
        <activity android:name=".view.activities.AddMovimentationActivity" />
        <activity android:name=".view.activities.MainActivity" />
        <activity android:name=".view.activities.ForgetPasswordActivity" />
        <activity android:name=".view.activities.RegisterActivity" />
        <activity android:name=".view.activities.LoginActivity" />
        <activity android:name=".view.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>